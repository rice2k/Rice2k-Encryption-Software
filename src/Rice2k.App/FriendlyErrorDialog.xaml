<Window x:Class="Rice2k.Encryption.FriendlyErrorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Rice2k Encryption Software"
        Width="620" SizeToContent="Height"
        MinHeight="260" MaxHeight="720"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        ShowInTaskbar="False">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Border Width="44" Height="44" CornerRadius="22" Background="#3A2214" VerticalAlignment="Top" Margin="0,0,14,0">
                <TextBlock Text="!" Foreground="{StaticResource WarningBrush}" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
            <StackPanel Grid.Column="1">
                <TextBlock x:Name="HeadingText" Text="Rice2k needs your attention" FontSize="22" FontWeight="SemiBold" TextWrapping="Wrap" />
                <TextBlock x:Name="MessageText" TextWrapping="Wrap" Margin="0,7,0,0" FontSize="14" />
            </StackPanel>
        </Grid>

        <Border x:Name="SuggestionPanel" Grid.Row="1" Background="#0D1728" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,18,0,0">
            <StackPanel>
                <TextBlock Text="What you can do" FontWeight="SemiBold" />
                <TextBlock x:Name="SuggestionText" TextWrapping="Wrap" Foreground="{StaticResource MutedTextBrush}" Margin="0,5,0,0" />
            </StackPanel>
        </Border>

        <Expander x:Name="TechnicalExpander" Grid.Row="2" Header="Technical details" Margin="0,16,0,0" Foreground="{StaticResource TextBrush}">
            <Border Background="#08101C" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="10" Margin="0,8,0,0">
                <ScrollViewer MaxHeight="260" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                    <TextBox x:Name="TechnicalDetailsText"
                             IsReadOnly="True"
                             BorderThickness="0"
                             Background="Transparent"
                             Foreground="{StaticResource MutedTextBrush}"
                             FontFamily="Cascadia Mono, Consolas"
                             FontSize="12"
                             TextWrapping="NoWrap" />
                </ScrollViewer>
            </Border>
        </Expander>

        <Grid Grid.Row="3" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Button x:Name="CopyDetailsButton" Content="Copy technical details" Click="CopyDetails_Click" Style="{StaticResource SecondaryButtonStyle}" HorizontalAlignment="Left" />
            <Button Grid.Column="1" Content="OK" IsDefault="True" Click="Ok_Click" MinWidth="95" />
        </Grid>
    </Grid>
</Window>
